#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @n: The int to be check.
 * Return: Absolute value int
 */

int _abs(int n)

{

	if (n < 0)
	{

	return (n);
	}
	return (-n);

}
